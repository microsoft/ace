<Grid Name="grid">
    <!-- Define a 3x3 grid with a center cell twice the size -->
    <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition Height="2*" />
        <RowDefinition />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition Width="2*" />
        <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <!-- Row 0 -->
    <Button                                     Background="Red">1</Button>
    <Button                     Grid.Column="1" Background="Orange">2</Button>
    <Button                     Grid.Column="2" Background="Yellow">3</Button>
    <!-- Row 1 -->
    <Button        Grid.Row="1"                 Background="Green" />
    <ToggleSwitch  Grid.Row="1" />
    <Button        Grid.Row="1" Grid.Column="2" Background="Aqua">5</Button>
    <!-- Row 2 -->
    <Button        Grid.Row="2"                 Background="Purple">6</Button>
    <Button        Grid.Row="2" Grid.Column="1" Background="Brown">7</Button>
    <WebView       Grid.Row="2" Grid.Column="2" Source="http://bing.com" />
</Grid>